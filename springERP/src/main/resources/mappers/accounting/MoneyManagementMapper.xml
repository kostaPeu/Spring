<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="erp.acc.mapper.MoneyManageMentMapper">

  <!-- MoneyManageMent CRUD START -->
	<insert id="moneyManageMentInsert" parameterType="MoneyManagement">
		insert into cash_trade (
		type, statement_id, dept_id, deal_name, proj_id, account_number, sums,
		charge, remarks, customer_id
		) values(
		#{type}, #{statement_id}, #{dept_id},
		#{deal_name}, #{proj_id}, #{account_number}, #{sums}, #{charge}, #{remarks}, #{customer_id}
		)
	</insert>

	<select id="moneyManageMentList" resultType="MoneyManagement">
		SELECT * FROM cash_trade
	</select>
	
	<!-- MoneyManageMent CRUD END -->
  
</mapper>



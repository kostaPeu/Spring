<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="erp.acc.mapper.Statement">

	<select id="StatementAllList" resultType="Statement">

		SELECT c.red_date,
		n.red_date, sums, charge, total FROM cash_trade T, note_ledger N, customer C,  where
		n.state = '지급' AND c.type = '매입' AND n.typelist = '지급어음'

	</select>
	
	<select id="">
	
	SELECT * FROM 
	
	</select>
</mapper>